{
  "name": "server",
  "version": "1.0.0",
  "description": "A single Cloudflare Worker that handles email-OTP and Telegram login for the app.",
  "main": "worker.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
